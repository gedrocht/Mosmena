# API Documentation

Mosmena publishes API reference material generated by Dokka.

When GitHub Pages is built, the generated API HTML is copied into:

- `api/reference/index.html`

If you are reading the repository locally, generate the reference with:

```bash
./gradlew :app:dokkaGeneratePublicationHtml
```
